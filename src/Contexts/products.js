import { createContext } from "react";

  const productContext = createContext({});
  //yek option ham darim ke mitonim inja benevisim ke chejor etelati daron in context darim! mitonim ham anjam nadim hich eshkali nadare

export default productContext;